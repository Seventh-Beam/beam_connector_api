# Proto

Protocol Buffer definitions for gRPC services.

## Structure

- `payment/v1/` - Payment service definitions

## Generating Code

Run `make proto-gen` to generate Go code from these definitions.